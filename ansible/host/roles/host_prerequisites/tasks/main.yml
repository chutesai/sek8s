---
- name: Install host script dependencies (aria2, PyYAML, python3-venv)
  ansible.builtin.apt:
    name:
      - aria2
      - python3-yaml
      - python3-venv
    state: present
    update_cache: true
