---
- name: Install host script dependencies
  ansible.builtin.apt:
    name:
      - git
      - aria2
      - python3-yaml
      - python3-venv
      - xfsprogs
      - numactl
    state: present
    update_cache: true
