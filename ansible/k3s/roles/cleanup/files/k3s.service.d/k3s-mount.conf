[Unit]
# Ensure k3s doesn't start until containerd cache and server dir are mounted (via bind mount)
# This is added at runtime (cleanup phase) because the storage volume doesn't exist during build
RequiresMountsFor=/var/lib/rancher/k3s/agent/containerd
RequiresMountsFor=/var/lib/rancher/k3s/server
After=cache-storage.mount
After=setup-storage-bind-mounts.service
