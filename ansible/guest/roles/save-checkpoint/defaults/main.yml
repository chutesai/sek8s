---
# Required: path where the checkpoint image will be saved.
# Set via `vars:` when including this role, e.g.:
#   vars:
#     checkpoint_img_path: "{{ prepared_img_path }}"
checkpoint_img_path: ""

# Build VM network — must match run-vm defaults. Override via inventory group_vars
# or host_vars to use a different libvirt subnet.
build_vm_ip: "192.168.122.176"
