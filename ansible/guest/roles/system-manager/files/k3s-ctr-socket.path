[Unit]
Description=Watch for k3s containerd socket
# Omit default Before=paths.target to avoid ordering cycles with opa/multi-user
DefaultDependencies=no
After=k3s.service

[Path]
PathExists=/run/k3s/containerd/containerd.sock

[Install]
WantedBy=multi-user.target
